# We use c for the main project.
language: c
compiler: gcc

install: 
  - sudo add-apt-repository -y ppa:terry.guo/gcc-arm-embedded
  - sudo apt-get update
  - sudo apt-get install gcc-arm-none-eabi
  - arm-none-eabi-gcc --version ;

script: make all
